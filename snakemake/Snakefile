rule add_parameters_and_normalize:
    input:
        "{BASE_PATH}/{BASE_NAME}.laz"
    output:
        "{BASE_PATH}/output/{BASE_NAME}_normalized.las"
    shell:
        "srun python ../workflow/01/01_add_parameters_and_normalize.py {input} {output}"
